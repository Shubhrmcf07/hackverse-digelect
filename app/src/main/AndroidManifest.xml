<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.digilect">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/digilect_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/digilect_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Digilect">
        <activity android:name=".EndSplashScreen"></activity>
        <activity android:name=".PartyActivity" />
        <activity android:name=".PhotoVerification" />
        <activity android:name=".VoterProfile" />
        <activity
            android:name=".VoterInput"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>