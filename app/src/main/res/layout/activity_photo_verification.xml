<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    tools:context=".PhotoVerification">
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Verify yourself by clicking and submitting a picture from your front facing camera."
        android:textColor="@color/blue"
        android:layout_margin="10dp"
        android:fontFamily="@font/montserrat"
        android:textSize="24sp"
        android:id="@+id/verifytext"
        android:gravity="center"/>
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/verifytext"
        android:layout_centerHorizontal="true"
        android:backgroundTint="@color/purple_500"
        android:text="capture"
        android:id="@+id/click"/>
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="50dp"
        android:id="@+id/image"
        android:layout_marginBottom="200dp"
        android:visibility="gone"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/blue"
        android:fontFamily="@font/montserrat"
        android:textSize="16sp"
        android:id="@+id/analysisresult"
        android:layout_below="@+id/image"
        android:visibility="gone"
        android:gravity="center"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/analysisresult"
        android:layout_centerHorizontal="true"
        android:visibility="gone"
        android:backgroundTint="@color/purple_500"
        android:text="proceed"
        android:id="@+id/proceed"/>

</RelativeLayout>